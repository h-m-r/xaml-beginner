<Page
    x:Class="RestaurantManager.ExpeditePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RestaurantManager"
    xmlns:models="using:RestaurantManager.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.TopAppBar>
        <AppBar>
            <StackPanel Orientation="Horizontal" Padding="5">
            <AppBarButton Icon="Home" Label="Home" Click="goHome"/>
            </StackPanel>
        </AppBar>
    </Page.TopAppBar>

    <Page.DataContext>
        <models:DataManager/>
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.9*"/>
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>
        
        <GridView Grid.Row="0" Margin="5"
                  ItemsSource="{Binding OrderItems}"/>
                
        <Button Content="Clear All Orders" Grid.Row="1" Click="ClearAll"
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
        
    </Grid>
</Page>
